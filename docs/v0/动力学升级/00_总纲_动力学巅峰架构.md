# 00 · 总纲：动力学加厚架构（修订 v0.2）

> 先读 [00_现状审计与增量原则.md](./00_现状审计与增量原则.md)

## 1. 文档目的

定义在 **不重建** `life_v0` 已有器官的前提下，把理论要求的连续调制、竞争整合、检索改写推到工程上限。

「动力学巅峰」= **增量加厚上限**，不是第二套生命内核。

## 2. 现状：已有主链（勿重复建设）

```text
external_utterance
  → live_turn_cycle
      → refresh_live_language_turn（五件套）
      → project_memory_retrieval_from_live_turn
      → refresh_pre_expression_consciousness
      → compose_model_expression（可选声带）
      → write_resident_turn_writeback（40+ project_*）
  → heartbeat / idle_strategy（等待期 30+ 状态摄入）
  → resident_autonomous_activity（sleep/recall/think/growth/learn）
```

**已厚、勿拆：** `resident_turn_writeback`、`memory U1–U25`、`ITR-08` 意识 live 链、Queue E→预测栈、DreamFactGate。

## 3. 真实断点（仅补这些）

| # | 断点 | 增量补法 |
|---|------|----------|
| 1 | 无 `dt` 积分 | `body/body_integrator.py` + heartbeat/turn 钩子 |
| 2 | body 主文件 stale | 回合末 integrate → 写 `core_affect` / `need_state` |
| 3 | 预测栈 stale | 回合末 `project_prediction_stack_from_live_turn` |
| 4 | signal_media 不随回合刷新 | 同上链末尾重算 |
| 5 | 检索少改写 | 扩展 `memory_trace_store` salience 钩子 |
| 6 | 工作区无竞争 | `workspace.py` 内 top-K |
| 7 | 无 chronnectome 日志 | `network_state` append jsonl |

## 4. 总原则

### 4.1 三层身体（不变）

| 身体 | 加厚方式 |
|------|----------|
| SiliconBody | `BodyIntegrator` 积分，**投影到**现有 `body/*.json` |
| NeuralLifeCore | 加厚 `project_*`，不新建平行帧 |
| ComputerBody | LLM = 可选声带；视觉 = 可选 encoder |

### 4.2 LLM

```text
决策：ExpressionPlan + expression_monitor + body/signal gates（已有）
加厚：plan 槽位读 workspace winner + integrator 标量
声带：model_expression（已有），输入压缩 intent，非 40 JSON dump
```

### 4.3 对象权威

```text
主连续状态（新增）: runtime/state/body/body_integrator_state.json
现有命名空间: 保留，由 integrator 推导或回合末 sync
禁止: 单独 life_state_vector 取代 life_state.json + body/*
```

与宏架构对象名对齐：`AllostaticForecast`、`BodyStateDebt` 作为 **integrator 内字段**，首写权威在 `body/body_integrator.py`。

## 5. 加厚主循环（修订）

```text
[已有] live_turn_cycle 前半
[新增] integrate_body_state(dt_since_last_tick)
[新增] refresh_signal_media_from_integrator()
[新增] project_prediction_stack_from_live_turn()
[已有] memory_retrieval + consciousness refresh
[新增] workspace_apply_topk_competition()  # flag: WORKSPACE_TOPK
[已有] expression + model_expression
[已有] write_resident_turn_writeback
[新增] append_network_chronnectome_log()
```

**后台（已有 heartbeat，加厚）：**

```text
heartbeat → integrate_body_state(background_dt) → idle_strategy（已有）
```

## 6. 全模块对齐矩阵（修订预期）

| 理论目标 | schema 现状 | 加厚后（v0.2 峰值） | 生物人脑 |
|----------|-------------|---------------------|----------|
| 记忆重构性召回 | **中偏高**（U13/U9 已有） | **高** | 极高 |
| 再巩固 | **中**（反馈/离线已有） | **中高** | 极高 |
| 内环境调制全链 | 低 | **中高** | 极高 |
| 意识可报告性 | 中 | **中高** | ？ |
| 情绪随身体变 | 低 | **中** | 极高 |
| 梦境事实边界 | **高** | **高** | 高 |
| 关系共同历史 | **高** | **高** | 高 |
| 语言消费全身 | 中 | **高** | 高 |
| 自主节律/主动发话 | **中偏高**（idle 厚） | **中高** | 高 |
| 神经振荡/扩散 | 低（声明性 diffusion_routes） | **低–中** | 有 |
| 用户上下文 | 中 | **高** | 高 |
| 认知形成 | 中 | **中高** | 极高 |
| 人格/自我 | 中 | **中高** | 高 |
| 视觉 | 低 | **中** | 极高 |
| 学习成长 | **高**（结构） | **中高** | 中 |

**修订说明：** 记忆/关系/梦境/idle 的 schema 列上调——代码审计显示已有闭合链，原矩阵低估。

## 7. 理论对象 → 代码落点（加厚，非新建包）

| 宏架构对象 | 落点 |
|------------|------|
| `AllostaticForecast` | `body/body_integrator.py` 字段 |
| `BodyStateDebt` | 同上，`debt_channels` |
| `BodyRhythmPulse` tick | `rhythm.py` 读 integrator.tick_counter |
| `SignalDiffusionField` | `signal_media.py` 增加 `integrate_modulation_from_integrator()` |
| `NetworkSwitchEvent` | 已有；加 `chronnectome_log.jsonl` |
| `ChronnectomeLog` | `neural_core/network_state.py` append |
| `ExpectedFreeEnergyEstimate` | `active_sampling.py` 增加 `efe_score` 字段（启发式） |
| Engram 激活 | `hippocampal_cue_index` + `memory_trace_store` |

## 8. 与 ITR / Queue 关系

```text
当前前沿: 十点第 10 点总核验 / ITR-09 动力学（本目录）
前置闭合: ITR-08、记忆 U25、predictive integration wave 深消费
动力学微段: ITR-09-A ~ F（见 13）
```

**不替代** `v0_current_iteration_plan` 三波循环；在其上 **加一段加厚波**。

## 9. 巅峰验收（修订）

| 类型 | 标准 |
|------|------|
| **Merge gate** | 各 ITR-09 微段测试 + 147 memory 回归 + 20 轮 smoke |
| **审计夹具** | 72h 无 LLM：节律/抑制/债务可观测 |
| **非 gate** | `human_brain_alignment` 夹具 ~78% 封顶不变 |

## 10. 阅读下一篇

[13_实施波次_测试与Gate.md](./13_实施波次_测试与Gate.md) — ITR-09-A 第一段即可开工。